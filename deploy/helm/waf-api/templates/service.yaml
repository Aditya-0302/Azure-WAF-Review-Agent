apiVersion: v1
kind: Service
metadata:
  name: {{ include "waf-api.fullname" . }}
  labels:
    {{- include "waf-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "waf-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
