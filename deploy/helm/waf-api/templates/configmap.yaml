apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "waf-api.fullname" . }}
  labels:
    {{- include "waf-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
