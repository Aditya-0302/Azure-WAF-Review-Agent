"""Shared fixtures for end-to-end tests.

E2E tests require a real deployed environment.  Configuration is read from
environment variables (see test_assessment_lifecycle.py for the full list).
These tests are skipped automatically when the required env vars are absent.
"""
